"use client"

export const BarChart = ({ data }: { data: any }) => {
  return (
    <div className="h-full w-full">
      {/* This is a placeholder component that gets implemented in compare-tools.tsx */}
      <div className="h-full w-full" />
    </div>
  )
}

export const LineChart = ({ data }: { data: any }) => {
  return (
    <div className="h-full w-full">
      {/* This is a placeholder component that gets implemented in compare-tools.tsx */}
      <div className="h-full w-full" />
    </div>
  )
}

export const PieChart = ({ data }: { data: any }) => {
  return (
    <div className="h-full w-full">
      {/* This is a placeholder component that gets implemented in compare-tools.tsx */}
      <div className="h-full w-full" />
    </div>
  )
}

export const RadarChart = ({ data }: { data: any }) => {
  return (
    <div className="h-full w-full">
      {/* This is a placeholder component that gets implemented in compare-tools.tsx */}
      <div className="h-full w-full" />
    </div>
  )
}

